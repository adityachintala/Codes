var readline = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
});

readline.question("Enter a sentence? ", function(line) {
    let arr = line.split(" ");
    func(arr);
    readline.close();
});


function func(arr) {
    console.log("The number of words : " + arr.length);
}




















































































































































































































































































































































































































































































































































































































































































































































































''
const configData = require('./data.json');
// console.log(typeof configData);
// console.log(configData);
// configData.push({ name: 'John', age: 30 });

// // to write data to data.json
// const fs = require('fs');
// fs.writeFileSync('./data.json', JSON.stringify(configData));
